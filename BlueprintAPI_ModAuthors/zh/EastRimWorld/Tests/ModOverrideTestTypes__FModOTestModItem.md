# `struct` `FModOTestModItem`

**源码头文件:** `EastRimWorld/Tests/ModOverrideTestTypes.h`

---

## 功能说明（来自头文件注释）

> Mod O Test Mod Item 数据结构。

## 蓝图暴露变量

### 属性 `Id`

| 项目 | 内容 |
|------|------|
| C++ 类型 | `FName` |
| 反射说明符 | （仅蓝图可见相关标记） |
| 蓝图侧含义 | 变量暴露给蓝图侧；具体能否在编辑器默认值中修改取决于 Edit 说明符。 |
| 原始声明（单行节选） | `UPROPERTY() FName Id;` |

**说明:**

> Id 字段。

---

### 属性 `Value`

| 项目 | 内容 |
|------|------|
| C++ 类型 | `int32` |
| 反射说明符 | （仅蓝图可见相关标记） |
| 蓝图侧含义 | 变量暴露给蓝图侧；具体能否在编辑器默认值中修改取决于 Edit 说明符。 |
| 原始声明（单行节选） | `UPROPERTY() int32 Value = 0;` |

**说明:**

> Value 字段。

---

### 属性 `ExtraField`

| 项目 | 内容 |
|------|------|
| C++ 类型 | `float` |
| 反射说明符 | （仅蓝图可见相关标记） |
| 蓝图侧含义 | 变量暴露给蓝图侧；具体能否在编辑器默认值中修改取决于 Edit 说明符。 |
| 原始声明（单行节选） | `UPROPERTY() float ExtraField = 0.f;` |

**说明:**

> Mod 多出来的字段，Runtime 没有对应字段时应被忽略而不是报错

---
