# `enum` `EGWOSectorVisual`

**源码头文件:** `EastRimWorld/SpawnActorSystem/GWOTypes.h`

---

## 功能说明（来自头文件注释）

> 扇区调试可视化的显示类别

## 枚举值

| 值 | 显示名 | 说明 |
|------|------|------|
| `CENTER` | Center | 以中心扇区样式显示 |
| `SIDE` | Side | 以周边扇区样式显示（默认值） |
| `CACHE` | Cache | 以缓存扇区样式显示 |
| `IDLE` | Idle | 以闲置扇区样式显示 |
