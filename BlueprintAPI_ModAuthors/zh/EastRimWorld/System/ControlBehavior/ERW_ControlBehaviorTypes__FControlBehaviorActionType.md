# `struct` `FControlBehaviorActionType`

**源码头文件:** `EastRimWorld/System/ControlBehavior/ERW_ControlBehaviorTypes.h`

---

## 功能说明（来自头文件注释）

> 行为动作类型（公共按钮 / 不发布类 GOAP 行为）

## 蓝图暴露变量

### 属性 `CommonButtonType`

| 项目 | 内容 |
|------|------|
| C++ 类型 | [ECommonButtonType](../../UI/Struct/UIStruct__ECommonButtonType.md) |
| 反射说明符 | BlueprintReadOnly |
| 蓝图侧含义 | 蓝图 **只读**，不可直接赋值。 |
| 原始声明（单行节选） | `UPROPERTY(BlueprintReadOnly) ECommonButtonType CommonButtonType = ECommonButtonType::None;` |

**说明:**

> Common Button Type 字段。

---

### 属性 `NoPublishActionType`

| 项目 | 内容 |
|------|------|
| C++ 类型 | [ENoPublishActionType](ERW_ControlBehaviorTypes__ENoPublishActionType.md) |
| 反射说明符 | BlueprintReadOnly |
| 蓝图侧含义 | 蓝图 **只读**，不可直接赋值。 |
| 原始声明（单行节选） | `UPROPERTY(BlueprintReadOnly) ENoPublishActionType NoPublishActionType = ENoPublishActionType::ControlBehavior_ActionType_None;` |

**说明:**

> No Publish Action Type 字段。

---
