---Experimental configuration to generate vertical links.
---@class FNavLinkGenerationJumpDownConfig
---@field public bEnabled boolean @Should this config be used to generate links. // @@todo Hidden for now. Since there is currently only one config, it has no real usage.
---@field public JumpLength number @Horizontal length of the jump. How far from the starting point we will look for ground.
---@field public JumpDistanceFromEdge number @How far from the navmesh edge is the jump started.
---@field public JumpMaxDepth number @How far below the starting height we want to look for landing ground.
---@field public JumpHeight number @Peak height relative to the height of the starting point.
---@field public JumpEndsHeightTolerance number @Tolerance at both ends of the jump to find ground.
---@field public SamplingSeparationFactor number @Value multiplied by CellSize to find the distance between sampling trajectories. Default is 1. Larger values improve generation speed but might introduce sampling errors.
---@field public FilterDistanceThreshold number @When filtering similar links, it's the distance used to compare between segment endpoints to match similar links. Use greater distance for more filtering (0 to deactivate filtering).
---@field public LinkBuilderFlags integer @Flags used to indicate how links will be added.
---@field public DownDirectionAreaClass TSubclassOf<UNavAreaBase> @Area class for downward traversal of links generated by this configuration. If the value is null, no link will be generated in this direction
---@field public UpDirectionAreaClass TSubclassOf<UNavAreaBase> @Area class for upward traversal of links generated by this configuration. If the value is null, no link will be generated in this direction
---@field public LinkProxyClass TSubclassOf<UBaseGeneratedNavLinksProxy> @Class used to handle links made with this configuration. Using this allows to implement custom behaviors when using navlinks, for example during the pathfollow. Note that having a proxy is not required for successful navlink pathfinding, but it does allow for custom behavior at the start and the end of a given navlink. This implies that using LinkProxyClass is optional, and it can remain empty (the default value).
---@field public LinkProxyId FNavLinkId @Identifier used identify the current proxy handler. All links generated through this config will use the same handler.
---@field public LinkProxy UBaseGeneratedNavLinksProxy @Current proxy. The proxy instance is build from the LinkProxyClass (provided it's not null). A proxy will be created if a @@see LinkProxyClass is used.
---@field public bLinkProxyRegistered boolean @Is the link proxy registered to the navigation system CustomNavLinksMap. Registration occurs on PostRegisterAllComponents or on PostLoadPreRebuild if a new proxy was created.
local FNavLinkGenerationJumpDownConfig = {}
