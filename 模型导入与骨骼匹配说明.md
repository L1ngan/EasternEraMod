# 模型导入与骨骼匹配说明

本文档仅讨论 **DCC 模型 → 虚幻引擎** 的流程，以及 **`USkeleton` 与 `USkeletalMesh`、动画资源** 的匹配关系，并明确 **主角 Mannequin 约束** 与 **动物/怪物独立骨架** 的工程约定。

---

## 1. 为何骨骼必须对齐

游戏中角色、动物、部分设施使用 **骨骼网格体（Skeletal Mesh）**。顶点通过 **蒙皮权重** 跟随骨骼变换；**动画序列 / 蒙太奇 / 混合空间** 等资源则编译在特定的 **`USkeleton`** 上。

若网格指向的 Skeleton 与动画资源不一致，会出现：**无法播放**、**静默套用失败**、**严重穿插扭曲** 等问题。因此「导入模型」的本质往往是 **把网格绑定到正确的 Skeleton 上**，而不是只导入几何。

---

## 2. 工程约定：主角与动物 / 怪物

| 类型 | 骨骼要求 |
|------|----------|
| **主角（玩家可操作的人类角色及同类管线模型）** | **必须**与工程规定的 Mannequin **参考骨骼**一致。该参考资产为 **`USkeleton`**，路径：`Content/Art/Animations/Characters/Mannequins/Meshes/SK_Mannequin.uasset`（内容浏览器中直接打开该 **骨骼** 资源即可，勿与引用此骨架的骨骼网格体混淆）。导入主角用 FBX 为 **Skeletal Mesh** 时，勾选 **使用现有 Skeleton**，并 **指定上述 `USkeleton` 资产**。禁止为主角使用与 Mannequin 不兼容的独立骨架，否则无法与游戏内人类动画、装备挂点、状态机对齐。 |
| **动物、怪物等** | **可以**使用 **独立骨架**：导入时不指定 Mannequin，由引擎生成或维护专用 `USkeleton`，并 **自行提供** 与之匹配的 BlendSpace、蒙太奇等；在数据侧用 **`AnimGroup`** 等与分组动画逻辑对齐（见数据表说明文档）。 |

后文「方式 A / 方式 B」的适用性**以上表为准**。

---

## 3. 核心概念（三个资源）

1. **`USkeleton`**：骨骼层级、参考姿势、插槽、重定向链等；动画资产与骨架绑定。  
2. **`USkeletalMesh`**：蒙皮后的几何，**必须引用一个** `USkeleton`。  
3. **动画**（`UAnimSequence`、`UAnimMontage`、`BlendSpace` 等）：针对某一 Skeleton（或经 IK Retarget 映射后的等价链）。

**一致性规则**：配置表里引用的一切动画资源，必须与该行 **`SkeletalMesh` 的 Skeleton** 兼容。**主角**在此基础上还必须等于 Mannequin 的 Skeleton。

---

## 4. DCC（Blender / Maya 等）制作要点

1. **绑定与蒙皮**：单一骨架、权重完整；骨骼 **命名与层级** 尽量与目标 Skeleton（Mannequin 或参考动物骨架）一致。  
2. **单位与轴向**：按工程常用 FBX 设置导出（常见为厘米、与 UE 导入轴向一致），避免整体缩放错误；动物可在表里用 `MeshScale` / `MeshLocationOffset` 微调显示，不改变骨架语义。  
3. **参考姿势（Bind / Reference Pose）**：与游戏/Mannequin 约定一致（常见 T-Pose）；导入后在骨架网格编辑器检查 **Reference Pose**。

---

## 5. 导入虚幻的两种骨架策略

### 5.1 方式 A：使用现有 Skeleton（主角必选；其它角色在需沿用游戏动画时选用）

1. **主角**：`USkeletalMesh` 的 **Skeleton** 必须 **固定**为 §2 中 Mannequin 参考路径所指的 **`USkeleton`** 资产。在 DCC 中使用与该骨架匹配的骨骼集合并导出。  
2. **非主角**：在工程中找一个 **同管线参考网格或 Skeleton**（例如同类动物）。  
3. FBX 导入类型选择 **Skeletal Mesh**，勾选 **Use Existing Skeleton**，选定目标 `USkeleton`。  
4. 骨骼名称/层级与 Skeleton 不一致时，导入可能失败或权重错乱，需在 DCC 侧修正。

### 5.2 方式 B：独立 Skeleton（动物、怪物等；**禁止用于主角**）

1. 导入网格时不绑定 Mannequin，生成（或指定）**新的** `USkeleton`。  
2. 为该 Skeleton **完整制作或重定向** 所需动画，并在 Mod 数据里 **只引用这一套** 资源。  
3. 通过 **`AnimGroup`** 等与程序侧约定区分，不要与人类 Mannequin 动画混用。

### 5.3 动画 FBX

导入动画时 **必须** 指定 **与目标网格相同的 Skeleton**；否则动画不会驱动你的网格。

### 5.4 替换官方网格的稳妥做法

在内容浏览器中 **复制或定位** 参考 `SkeletalMesh` → 对你的 FBX 使用 **Reimport** 或 **同 Skeleton 重新导入**，可减少骨架与路径漂移。

---

## 6. 骨骼不一致时的修正（概要）

当需要在「略有差异的骨架」上复用动画时：

- 使用 UE5 **IK Rig + Retargeter** 或项目认可的 **动画重定向** 管线，得到针对目标 Skeleton 的动画资产；  
- Mod 内引用 **重定向后的动画**，并保证 **AnimInstance / AnimBP** 与目标 Skeleton 一致。

---

## 7. 导入后自检清单

- [ ] `USkeletalMesh` → **Skeleton** 是否为预期（主角须与 Mannequin 一致）。  
- [ ] 在编辑器预览 BlendSpace / Montage 时无严重扭曲、撕裂。  
- [ ] 设施类：若使用 `AnimInstance`，其对应 AnimBP 是否针对该 Mesh 的 Skeleton。

---

## 8. 常见问题（模型与骨骼）

| 现象 | 可能原因 | 处理方向 |
|------|----------|----------|
| 导入后没有动画或静止 | Skeleton 与 `AnimSequence` 等不一致 | 网格与动画导入时统一指定正确 Skeleton |
| 肢体扭曲、穿插 | 骨骼名/层级/权重与 Skeleton 不匹配 | 对照参考 Skeleton 修正 DCC；检查 Bind Pose |
| 部分动物异常 | `AnimGroup` 与动画资源管线不对应 | 与项目约定对齐分组或整套替换动画 |
| 主角动作异常或无法装备 | 未使用 Mannequin 参考 `USkeleton` | 按 §2、§5.1 在导入时指定 `Content/Art/.../SK_Mannequin.uasset` 所指的骨骼资产并重新导入网格 |
| 设施不播动画 | `AnimInstance` 与 Mesh 的 Skeleton 不匹配 | 更换兼容的 AnimBP 子类或修正 Mesh |

---

## 9. 相关文档

- [Mod文档索引.md](./Mod文档索引.md)  
- [Mod工程与目录说明.md](./Mod工程与目录说明.md)  
- [Mod数据表与骨骼网格字段说明.md](./Mod数据表与骨骼网格字段说明.md)

*若 UE 版本或导入对话框与本文描述不符，以引擎界面为准。*
